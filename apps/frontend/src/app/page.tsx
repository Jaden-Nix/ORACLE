import { OracleLayout } from "@/components/oracle/OracleLayout";

export const metadata = {
  title: "ORACLE",
  description: "An agent-directed cinematic generative UI for AI Tinkerers.",
};

export default function HomePage() {
  return <OracleLayout />;
}
